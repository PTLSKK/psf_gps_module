class UserLocation {
  final double latitude;
  final double longitude;
  final double speed;
  final double altitude;
  final double heading;

  UserLocation(
      {this.latitude, this.longitude, this.speed, this.altitude, this.heading});
}
